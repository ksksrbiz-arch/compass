# Compass — Environment Variables
# Copy this file to .env.local for development

# URL of the backend API proxy (Cloud Run or local Express server)
VITE_API_URL=http://localhost:3001

# --- Server-side only (server/.env) ---
# ANTHROPIC_API_KEY=sk-ant-...
# COMPASS_AUTH_TOKEN=your-static-bearer-token
# ALLOWED_ORIGINS=https://your-netlify-site.netlify.app,http://localhost:5173
